package org.vaadin.addons.autoselectcombobox;

/**
 * Defines which user actions trigger custom value handling when the typed
 * text does not match any item in the list.
 */
public enum CustomValueTrigger {
    ENTER,
    TAB,
    BLUR
}
